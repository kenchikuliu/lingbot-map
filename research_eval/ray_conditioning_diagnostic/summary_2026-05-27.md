# Ray-Conditioning Diagnostic Summary

Date: 2026-05-27

## Purpose

This is a diagnostic check for the new projection-aware input path, not a trained
performance claim. The ray-conditioning gate was manually overridden to `0.1` to
confirm that:

1. explicit intrinsics files are parsed and forwarded correctly, and
2. changing the camera geometry input changes the model's outputs.

## Setup

- Sequence: `tmp_frames/video_20260418_1906051_ffmpeg_frames_fps2`
- Frames: first 24
- Checkpoint: `checkpoints/lingbot-map.pt`
- Mode: streaming
- Scale frames: 8
- Backend: SDPA
- Ray-conditioning: enabled
- Gate override: `0.1`

## Inputs

- `pinhole_default`
  - camera model: `pinhole`
  - intrinsics: model's pseudo default
- `pinhole_intrinsics`
  - camera model: `pinhole`
  - intrinsics file:
    `research_eval/camera_inputs/video_20260418_pseudo_intrinsics_1920x1080.json`
- `equirectangular`
  - camera model: `equirectangular`
  - intrinsics: not used

## Proxy Metrics

| variant | step_rotation_deg_mean | step_translation_mean | depth_conf_mean | translation_norm_mean |
|---|---:|---:|---:|---:|
| pinhole_default | 0.569016 | 0.019705 | 3.171200 | 0.222280 |
| pinhole_intrinsics | 0.569686 | 0.019952 | 3.150721 | 0.224358 |
| equirectangular | 0.571291 | 0.020324 | 3.071786 | 0.228351 |

## Takeaway

- The explicit-intrinsics path is active: `pinhole_default` and
  `pinhole_intrinsics` are no longer identical once the diagnostic gate is
  opened.
- The camera-model path is active: `pinhole_intrinsics` and `equirectangular`
  also diverge under the same checkpoint and sequence.
- The effect is modest because this is still an untrained side channel with a
  small manual gate override. This result should be interpreted as wiring
  validation only.
